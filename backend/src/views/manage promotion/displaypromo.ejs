<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Display promotion</title>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td,
        th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }

        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
        }

        .topnav {
            overflow: hidden;
            background-color: #333;
        }

        .topnav a {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
        }

        .topnav a:hover {
            background-color: #ddd;
            color: black;
        }

        .topnav a.active {
            background-color: #04AA6D;
            color: white;
        }
    </style>
</head>

<body>
    <div class="topnav">
        <a href='/'>Home</a>
        <a href='/createpromo'>Create Promotion</a>
        <a href='/get_listpromo'>List Promotion</a>
    </div>
    <table>
        <tr>
            <th>Code</th>
            <th>description</th>
            <th>Quantity</th>
            <th>Create at</th>
            <th>Precent</th>
            <th>Action</th>
        </tr>

        <% for(let i=0; i < dataTable.length; i++) { %>
            <tr>
                <td>
                    <%= dataTable[i].voucher_code %>
                </td>
                <td>
                    <%= dataTable[i].desc %>
                </td>
                <td>
                    <%= dataTable[i].quantity %>
                </td>
                <td>
                    <%= dataTable[i].create_at %>
                </td>
                <td>
                    <%= dataTable[i].voucher_percent %>
                </td>
                <td>
                    <a href="/edit_promo?id=<%= dataTable[i].id %>" class="btn-edit" type="button">Edit</a>
                    <a href="/delete_promo?id=<%= dataTable[i].id %>" class="btn-delete" type="button">Delete</a>
                </td>
            </tr>
            <% } %>
    </table>
</body>

</html>